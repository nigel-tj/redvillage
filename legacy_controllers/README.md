# Legacy / quarantined controllers
#
# These files are intentionally outside `app/controllers` so Zeitwerk does not
# load them. They contain obsolete Rails 3-era code (and historically leaked
# credentials). Do not move them back into the live app load path.
#
# Prefer git history if you need to reference behavior.
