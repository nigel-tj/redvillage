<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-md-6 col-lg-5">
      <div class="card shadow-lg border-0 rounded-lg">
        <div class="card-header bg-gradient-primary text-white text-center py-4">
          <h2 class="mb-0">
            <i class="fas fa-user-plus mr-2"></i>
            Join The Red Cup Village
          </h2>
          <p class="mb-0 mt-2">Create your account to get started</p>
        </div>
        <div class="card-body p-4">
          <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: 'user' }) do |f| %>
            <%= render "devise/shared/error_messages", resource: resource %>

            <div class="form-group mb-4">
              <%= f.label :email, class: 'form-label text-dark font-weight-bold' %>
              <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control form-control-lg', placeholder: 'Enter your email address' %>
            </div>

            <div class="form-group mb-4">
              <%= f.label :password, class: 'form-label text-dark font-weight-bold' %>
              <%= f.password_field :password, autocomplete: "new-password", class: 'form-control form-control-lg', placeholder: 'Create a password (minimum 6 characters)' %>
            </div>

            <div class="form-group mb-4">
              <%= f.label :password_confirmation, class: 'form-label text-dark font-weight-bold' %>
              <%= f.password_field :password_confirmation, autocomplete: "new-password", class: 'form-control form-control-lg', placeholder: 'Confirm your password' %>
            </div>

            <div class="form-group mb-4">
              <%= f.submit "Create Account", class: 'btn btn-primary btn-lg btn-block font-weight-bold' %>
            </div>
          <% end %>

          <hr class="my-4">

          <div class="text-center">
            <%= render "devise/shared/links" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
.bg-gradient-primary {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.card {
  border-radius: 15px;
  overflow: hidden;
}

.form-control:focus {
  border-color: #667eea;
  box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
}

.btn-primary {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border: none;
  transition: all 0.3s ease;
}

.btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
}
</style>
