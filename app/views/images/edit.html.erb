<div class="dash">
  <%= render "shared/dash_page_header",
        title: "Edit Image",
        eyebrow: "Gallery workspace",
        subtitle: @image.name.presence || "Update image details",
        user: current_user %>

  <%= render layout: "dashboards/panel", locals: { title: "Image details", padded: true } do %>
    <%= render "form" %>
  <% end %>
</div>
